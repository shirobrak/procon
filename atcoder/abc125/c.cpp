#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;

int N;
vector<int> A;

int gcd(int x, int y) {
    int ans;
    return ans;
}

const int MAX_N = 100000;

void solve() {
    int ans;
    sort(A.begin(), A.end());
    for (int i = 0; i < N; i++) {

    }

    cout << ans << endl;
}

int main() {
    cin >> N;
    for (int i = 0; i < N; i++) {
        int tmp;
        cin >> tmp;
        A.push_back(tmp);
    }
    solve();
}